import { LibelleConcourPipe } from './libelle-concour.pipe';

describe('LibelleConcourPipe', () => {
  it('create an instance', () => {
    const pipe = new LibelleConcourPipe();
    expect(pipe).toBeTruthy();
  });
});
